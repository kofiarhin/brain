# Inbox

This file has been replaced by `notes.txt`.

Use `notes.txt` as the raw dumping ground for all unprocessed thoughts, notes, reminders, tasks, ideas, accomplishments, family updates, project updates, lessons, blockers, and observations.

When you say `update life`, the AI should process `notes.txt` and update the structured files automatically.
